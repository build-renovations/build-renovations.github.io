---
phase: 06-identity-contact-and-navigation-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase6_render_checks.mjs
  - scripts/qa.sh
  - _config.yml
  - _data/navigation.yml
autonomous: true
requirements:
  - TRST-01
  - CASE-03

must_haves:
  truths:
    - "`<title>` on UA and EN home pages shows 'Рівень', not 'Remonty'"
    - "`og:title` on UA and EN home pages shows 'Рівень', not 'Remonty'"
    - "Top-level nav on every page contains a link to `/projects/` (UA) and `/en/projects/` (EN)"
    - "`./scripts/qa.sh` exits 0 and includes Phase 6 render checks"
  artifacts:
    - path: "scripts/phase6_render_checks.mjs"
      provides: "Automated render checks for brand name and nav link presence"
      exports: ["main"]
    - path: "_config.yml"
      provides: "Corrected brand name in title, seo.uk.title, seo.en.title"
      contains: "title: Рівень"
    - path: "_data/navigation.yml"
      provides: "Projects nav entry for uk and en locales"
      contains: "/projects/"
  key_links:
    - from: "_config.yml seo.uk.title / seo.en.title"
      to: "_includes/head.html page_title assembly"
      via: "Liquid: site.seo.uk.title fallback path"
      pattern: "seo\\.uk\\.title"
    - from: "_data/navigation.yml uk/en arrays"
      to: "_includes/header.html nav loop"
      via: "Liquid: site.data.navigation[page.lang]"
      pattern: "navigation\\.yml"
    - from: "scripts/phase6_render_checks.mjs"
      to: "scripts/qa.sh"
      via: "node ./scripts/phase6_render_checks.mjs invocation"
      pattern: "phase6_render_checks"
---

<objective>
Create the Phase 6 render check script, wire it into qa.sh, fix the brand name in `_config.yml`, and add the projects nav links to `_data/navigation.yml`.

Purpose: Establish the automated gate before making the data changes it validates, then close the TRST-01 and CASE-03 gaps in a single plan.
Output: `scripts/phase6_render_checks.mjs`, updated `scripts/qa.sh`, corrected `_config.yml` title fields, and `/projects/` entries in both nav locale arrays.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-identity-contact-and-navigation-completion/06-RESEARCH.md
@.planning/phases/06-identity-contact-and-navigation-completion/06-VALIDATION.md
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From scripts/phase5_render_checks.mjs (reference implementation):
```javascript
// Pattern for fail helper, readHtml, and main loop — replicate exactly
function fail(message) { console.error(`FAIL: ${message}`); process.exit(1); }
function readHtml(route) { /* strips leading slash, reads _site/{route}/index.html */ }
// main() builds htmlCache, iterates checkedRoutes, calls check functions
```

From _config.yml (current — wrong values):
```yaml
title: Remonty                                          # → Рівень
seo:
  uk:
    title: "Remonty | Ремонт від сантехніки до фінального оздоблення"   # → Рівень | …
  en:
    title: "Remonty | Renovation from plumbing to final finishing"       # → Рівень | …
```

From _data/navigation.yml (current — missing projects):
```yaml
uk:
  - title: "Про нас"
    url: /about/
  - title: "Контакти"        # projects goes before this
    url: /contact/
en:
  - title: "About"
    url: /en/about/
  - title: "Contact"         # projects goes before this
    url: /en/contact/
```

From scripts/qa.sh (current — missing phase6 invocation):
```bash
node ./scripts/phase5_render_checks.mjs
# ADD: node ./scripts/phase6_render_checks.mjs
echo "Build and file checks passed."
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create phase6_render_checks.mjs and wire into qa.sh</name>
  <files>scripts/phase6_render_checks.mjs, scripts/qa.sh</files>
  <behavior>
    - checkBrandName("/", html): FAIL if `&lt;title&gt;` contains "Remonty"
    - checkBrandName("/en/", html): FAIL if `&lt;title&gt;` or og:title contains "Remonty"
    - checkProjectsNav("/", html): FAIL if nav does not contain `href="/projects/"`
    - checkProjectsNav("/en/", html): FAIL if nav does not contain `href="/en/projects/"`
    - checkNoPlaceholderMessengers("/contact/", html): FAIL if HTML contains "telegram_phase1_contact"
    - checkNoPlaceholderMessengers("/contact/", html): FAIL if HTML contains "AQExamplePhase1"
    - checkNoPlaceholderMessengers("/contact/", html): FAIL if HTML contains "send?phone=380000000000"
    - main() exits 0 and prints "Phase 6 render checks passed." when all pass
  </behavior>
  <action>
    Create `scripts/phase6_render_checks.mjs` following the Phase 5 script pattern exactly:

    1. Import `fs` and `path` from `node:*`.
    2. Declare `repoRoot = process.cwd()` and `siteRoot = path.join(repoRoot, "_site")`.
    3. `fail(message)`: console.error + process.exit(1).
    4. `readHtml(route)`: strip leading slash, join with siteRoot, root route reads `_site/index.html`, others read `_site/{route}/index.html`. Call `fail()` if file missing.
    5. `checkBrandName(route, html)`: match `&lt;title&gt;([^&lt;]*)&lt;/title&gt;` — if value includes "Remonty", fail. Match og:title content — if value includes "Remonty", fail.
    6. `checkProjectsNav(route, html)`: for "/" assert `html.includes('href="/projects/"')`, for "/en/" assert `html.includes('href="/en/projects/"')`.
    7. `checkNoPlaceholderMessengers(route, html)`: assert none of "telegram_phase1_contact", "AQExamplePhase1", "send?phone=380000000000" appear in html.
    8. `main()`: verify `_site` exists, read html for routes `["/", "/en/", "/contact/", "/en/contact/"]`, run checks, print pass message.

    Check routes to run per function:
    - checkBrandName: "/" and "/en/"
    - checkProjectsNav: "/" and "/en/"
    - checkNoPlaceholderMessengers: "/contact/" and "/en/contact/"

    After creating the script, update `scripts/qa.sh`: add `node ./scripts/phase6_render_checks.mjs` on a new line immediately after the existing `node ./scripts/phase5_render_checks.mjs` line.

    Note: At the time this script is created, it WILL fail (the data changes haven't landed yet). That is expected — the script should be correct; the data fix in Task 2 will turn it green.
  </action>
  <verify>
    <automated>node ./scripts/phase6_render_checks.mjs 2>&1 | head -5 || true</automated>
    Confirm the script exists and is syntactically valid: `node --check ./scripts/phase6_render_checks.mjs`. The script will fail functionally (brand name still wrong) — that is expected at this stage.
  </verify>
  <done>
    `scripts/phase6_render_checks.mjs` exists and passes `node --check`. `scripts/qa.sh` contains `node ./scripts/phase6_render_checks.mjs`. Script logic matches the behavior spec above.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix brand name in _config.yml and add projects to navigation.yml</name>
  <files>_config.yml, _data/navigation.yml</files>
  <action>
    **_config.yml — update three fields:**

    Change `title: Remonty` to `title: Рівень` (line 1).

    Change `seo.uk.title` from:
    `"Remonty | Ремонт від сантехніки до фінального оздоблення"`
    to:
    `"Рівень | Ремонт від сантехніки до фінального оздоблення"`

    Change `seo.en.title` from:
    `"Remonty | Renovation from plumbing to final finishing"`
    to:
    `"Рівень | Renovation from plumbing to final finishing"`

    Do not change any other fields.

    **_data/navigation.yml — add projects entry before "Контакти"/"Contact" in both locale arrays:**

    UK array — insert before the `Контакти` entry:
    ```yaml
      - title: "Проєкти"
        url: /projects/
    ```

    EN array — insert before the `Contact` entry:
    ```yaml
      - title: "Projects"
        url: /en/projects/
    ```

    Use trailing slashes on both URLs (consistent with all other nav entries). Position: before the contact link, after the "Про нас"/"About" entry — mirrors the trust-building flow (services → process → proof → about → contact).

    After editing, run a full build and qa.sh to verify green. Fix any issues before marking done.
  </action>
  <verify>
    <automated>RUBYOPT="-r./scripts/jekyll_compat.rb ${RUBYOPT:-}" bundle exec jekyll build --quiet && ./scripts/qa.sh</automated>
  </verify>
  <done>
    `./scripts/qa.sh` exits 0 with "Phase 6 render checks passed." in output. `_site/index.html` `&lt;title&gt;` contains "Рівень". `_site/en/index.html` `&lt;title&gt;` contains "Рівень". `_site/index.html` nav contains `href="/projects/"`. `_site/en/index.html` nav contains `href="/en/projects/"`.
  </done>
</task>

</tasks>

<verification>
Run `./scripts/qa.sh` — must exit 0 with "Phase 6 render checks passed." in output.

Spot-check rendered HTML:
- `grep -i "remonty" _site/index.html` — must return no matches inside `&lt;title&gt;` or og:title
- `grep -i "remonty" _site/en/index.html` — must return no matches inside `&lt;title&gt;` or og:title
- `grep 'href="/projects/"' _site/index.html` — must match
- `grep 'href="/en/projects/"' _site/en/index.html` — must match
</verification>

<success_criteria>
1. `scripts/phase6_render_checks.mjs` exists and passes `node --check`
2. `scripts/qa.sh` contains `node ./scripts/phase6_render_checks.mjs`
3. `_config.yml` has `title: Рівень`, `seo.uk.title` and `seo.en.title` use "Рівень"
4. `_data/navigation.yml` has "Проєкти" → `/projects/` in uk and "Projects" → `/en/projects/` in en
5. `./scripts/qa.sh` exits 0 after a fresh Jekyll build
</success_criteria>

<output>
After completion, create `.planning/phases/06-identity-contact-and-navigation-completion/06-01-SUMMARY.md`
</output>
