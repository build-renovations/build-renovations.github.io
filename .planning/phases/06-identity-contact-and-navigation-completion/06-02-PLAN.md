---
phase: 06-identity-contact-and-navigation-completion
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - _data/contact_channels.yml
autonomous: false
requirements:
  - CALL-04

user_setup:
  - service: messenger-handles
    why: "Real business Telegram, Viber, and WhatsApp handles are required to replace Phase 1 placeholders. These are owner-supplied values that Claude cannot derive from the codebase."
    env_vars: []
    dashboard_config:
      - task: "Provide Telegram username"
        location: "Your Telegram business account — the username after t.me/"
      - task: "Provide Viber invite link or phone number"
        location: "Your Viber business profile — either invite.viber.com/... link or E.164 phone for viber://chat?number="
      - task: "Provide WhatsApp phone number"
        location: "Your WhatsApp Business account phone in E.164 format without the leading + (e.g. 380977878523)"

must_haves:
  truths:
    - "All three messenger links on `/contact/` and `/en/contact/` point to real, non-placeholder handles"
    - "No placeholder strings (telegram_phase1_contact, AQExamplePhase1, send?phone=380000000000) appear in rendered HTML"
    - "`./scripts/qa.sh` exits 0 after the messenger URL update"
    - "Both the uk and en locale blocks in contact_channels.yml use identical real messenger URLs"
  artifacts:
    - path: "_data/contact_channels.yml"
      provides: "Real messenger URLs for Telegram, Viber, WhatsApp in both uk and en locale blocks"
      contains: "t.me/"
  key_links:
    - from: "_data/contact_channels.yml messengers[].url"
      to: "contact page rendered HTML"
      via: "Liquid include consuming contact_channels[page.lang].messengers"
      pattern: "contact_channels"
---

<objective>
Replace the three Phase 1 placeholder messenger URLs in `_data/contact_channels.yml` with real business handles for Telegram, Viber, and WhatsApp, in both the `uk` and `en` locale blocks.

Purpose: Closes CALL-04 — visitors reaching the contact page can now access real secondary fallback contact paths.
Output: Updated `_data/contact_channels.yml` with real handles; `./scripts/qa.sh` exits green.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-identity-contact-and-navigation-completion/06-RESEARCH.md
@.planning/phases/06-identity-contact-and-navigation-completion/06-01-SUMMARY.md
</context>

<interfaces>
<!-- Key contract for the executor. Extracted from _data/contact_channels.yml -->

Current placeholder values in both uk and en blocks:
```yaml
messengers:
  - name: Telegram
    label: "Telegram"
    url: "https://t.me/telegram_phase1_contact"         # → replace with real handle
  - name: Viber
    label: "Viber"
    url: "https://invite.viber.com/?g2=AQExamplePhase1" # → replace with real handle
  - name: WhatsApp
    label: "WhatsApp"
    url: "https://api.whatsapp.com/send?phone=380000000000" # → replace with real phone (no +)
```

URL formats for reference:
- Telegram: `https://t.me/USERNAME`
- Viber invite: `https://invite.viber.com/?g2=CODE` OR direct: `viber://chat?number=380XXXXXXXXX`
- WhatsApp: `https://api.whatsapp.com/send?phone=380XXXXXXXXX` (no + prefix on phone number)

Both uk and en blocks must receive identical messenger URL values (handles are locale-agnostic).
</interfaces>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Collect real messenger handles from owner</name>
  <files></files>
  <action>
    Phase 6 Plan 01 has landed: render checks are active, brand name and nav gaps are closed. This plan needs the real messenger handles to replace placeholders in contact_channels.yml. Request the following three values from the owner before proceeding to Task 2.
  </action>
  <verify>
    <automated>MISSING — this is an owner-input gate; no automated check possible until handles are provided</automated>
  </verify>
  <done>Owner has provided Telegram username, Viber link or phone, and WhatsApp phone number.</done>
  <how-to-verify>
    Provide the following three values for the business messenger channels:

    1. **Telegram username** — the handle after `t.me/` for the business account
       Example: if your link is `https://t.me/rivenlviv`, provide `rivenlviv`

    2. **Viber link or phone number** — either:
       - A Viber invite link: `https://invite.viber.com/?g2=XXXXXXXX`
       - Or the business phone in E.164 format for a direct message link: `380XXXXXXXXX`

    3. **WhatsApp phone number** — the E.164 number **without** the leading `+`
       Example: if the number is `+380977878523`, provide `380977878523`

    These are the only values needed. No dashboard configuration or account creation required — just the handles/numbers you already use.
  </how-to-verify>
  <resume-signal>Provide the three handles/numbers and type "ready" (or include them inline)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update contact_channels.yml with real messenger handles</name>
  <files>_data/contact_channels.yml</files>
  <action>
    Using the handles supplied in the Task 1 checkpoint, update `_data/contact_channels.yml`:

    **Replace in the `uk.messengers` array AND the `en.messengers` array (both blocks get identical URLs):**

    Telegram URL:
    - Old: `"https://t.me/telegram_phase1_contact"`
    - New: `"https://t.me/{TELEGRAM_USERNAME}"`

    Viber URL (choose format based on what the owner provided):
    - If an invite link was provided: use it directly
    - If a phone number was provided: `"viber://chat?number={E164_PHONE}"`
    - Old: `"https://invite.viber.com/?g2=AQExamplePhase1"`

    WhatsApp URL:
    - Old: `"https://api.whatsapp.com/send?phone=380000000000"`
    - New: `"https://api.whatsapp.com/send?phone={E164_NO_PLUS}"`
    - Critical: the phone must NOT have a `+` prefix in this URL

    All other fields (`name`, `label`, `primary_label`, `primary_support`, `sticky_label`, etc.) remain unchanged. Only the three `url:` values inside each `messengers:` array change.

    Pitfall check: verify BOTH the `uk:` and `en:` blocks are updated — both have independent `messengers:` arrays with the same three placeholder values.

    After editing, run the full build and qa.sh to verify green.
  </action>
  <verify>
    <automated>RUBYOPT="-r./scripts/jekyll_compat.rb ${RUBYOPT:-}" bundle exec jekyll build --quiet && ./scripts/qa.sh</automated>
  </verify>
  <done>
    `./scripts/qa.sh` exits 0. "Phase 6 render checks passed." appears in output. Neither "telegram_phase1_contact", "AQExamplePhase1", nor "send?phone=380000000000" appear in `_site/contact/index.html` or `_site/en/contact/index.html`. Both uk and en blocks in `contact_channels.yml` reference the real handles.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify messenger links resolve to live business profiles</name>
  <files></files>
  <action>Present the messenger URL update summary and request owner confirmation that all three links open to the correct business profiles. Automated checks confirm the placeholders are gone; this gate confirms the real handles are correct.</action>
  <verify>
    <automated>MISSING — this is a human-judgment gate; automated checks completed in Task 2</automated>
  </verify>
  <done>Owner has typed "approved" or confirmed that all three messenger links open to the correct business profiles.</done>
  <what-built>
    Task 2 updated `_data/contact_channels.yml`: all six placeholder URLs (uk and en blocks) replaced with real handles. `./scripts/qa.sh` exits 0 with Phase 6 render checks passing.
  </what-built>
  <how-to-verify>
    Open the rendered contact page in a browser (run `bundle exec jekyll serve` and visit `http://localhost:4000/contact/`):

    1. Click the Telegram link — confirm it opens to the correct business Telegram profile
    2. Click the Viber link — confirm it opens to the correct business Viber profile or prompts a direct message
    3. Click the WhatsApp link — confirm it opens to the correct business WhatsApp chat

    Repeat on `http://localhost:4000/en/contact/` to confirm EN page resolves to the same channels.

    If any link is wrong, provide the corrected handle and type "retry". If all links are correct, approve.
  </how-to-verify>
  <resume-signal>Type "approved" if all three messenger links resolve correctly, or describe what needs fixing</resume-signal>
</task>

</tasks>

<verification>
After Task 2 green:
- `./scripts/qa.sh` exits 0
- `grep "telegram_phase1_contact" _site/contact/index.html` returns no matches
- `grep "AQExamplePhase1" _site/contact/index.html` returns no matches
- `grep "380000000000" _site/contact/index.html` returns no matches
- Same checks pass on `_site/en/contact/index.html`

After Task 3 human checkpoint:
- All three messenger links confirmed live by owner
</verification>

<success_criteria>
1. `_data/contact_channels.yml` has real handles in both `uk.messengers` and `en.messengers` arrays
2. No placeholder messenger strings appear in rendered contact pages
3. `./scripts/qa.sh` exits 0
4. Owner has confirmed all three messenger links open to the correct business profiles
</success_criteria>

<output>
After completion, create `.planning/phases/06-identity-contact-and-navigation-completion/06-02-SUMMARY.md`
</output>
