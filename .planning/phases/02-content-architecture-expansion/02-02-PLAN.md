---
phase: 02-content-architecture-expansion
plan: 02
type: execute
tool_mode: repo_only
mcp_usage: intentionally_not_needed
wave: 2
depends_on:
  - 02-01
files_modified:
  - _data/service_pages.yml
  - _data/process_page.yml
  - _data/translations.yml
  - _layouts/service_detail.html
  - _layouts/process.html
  - _layouts/home.html
  - assets/css/site.css
  - scripts/qa.sh
  - scripts/phase2_render_checks.mjs
autonomous: true
requirements:
  - CONT-01
  - CONT-03
must_haves:
  truths:
    - Service-detail and process pages must carry the heaviest Phase 2 information density because buyers judge scope, control, timing, and seriousness there.
    - Richer content must stay scan-first through short intros, summaries, lists, and subsections rather than long uninterrupted prose.
    - Commercial clarity should explain planning, procurement, coordination, and handoff in calm public-facing language without centering mid-project change management.
    - Moderate repetition between home, services, and process is acceptable only when it supports trust and route selection rather than bloating every page equally.
  artifacts:
    - _data/service_pages.yml
    - _data/process_page.yml
    - _data/translations.yml
    - _layouts/service_detail.html
    - _layouts/process.html
    - _layouts/home.html
    - assets/css/site.css
    - scripts/phase2_render_checks.mjs
  key_links:
    - _data/service_pages.yml feeds the deeper service-detail sections that answer scope, outcomes, timing signals, coordination logic, and buyer-fit boundaries.
    - _data/process_page.yml supplies the process-page commercial clarity and project-control explanations required for CONT-03.
    - _layouts/service_detail.html and _layouts/process.html render the main density increase, while _layouts/home.html only promotes the strongest summaries and routes deeper reading.
    - _data/translations.yml may add only the shared UI labels needed for new section headings or navigation cues, not large duplicated editorial copy.
    - scripts/phase2_render_checks.mjs must fail if representative service/process routes lose the required depth and commercial-clarity sections in either language.
---

<objective>
Roll out the primary Phase 2 content-depth surfaces on service-detail and process routes, with only lightweight reinforcement on the homepage.

Purpose: This wave satisfies the core depth and commercial-clarity requirements where buyers make decisions, while preserving the site’s phone-first and scan-first behavior.
Output: Richer service/process content modules, limited homepage bridge content, and rendered verification for the main density surfaces.
MCP status: intentionally not needed because this work depends on locked repo decisions and existing page architecture rather than external research.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-content-architecture-expansion/02-CONTEXT.md
@.planning/phases/02-content-architecture-expansion/02-RESEARCH.md
@.planning/phases/02-content-architecture-expansion/02-VALIDATION.md
@.planning/phases/02-content-architecture-expansion/02-01-PLAN.md
@_data/service_pages.yml
@_data/process_page.yml
@_layouts/service_detail.html
@_layouts/process.html
@_layouts/home.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deepen service-page data around scope, fit, outcomes, and coordination</name>
  <files>_data/service_pages.yml</files>
  <action>Populate the richer service-page contract for the priority trade routes first. Apartment and house entries may receive only the minimum shared schema fields needed to stay compatible with the richer contract, but the real fit-guidance deepening for those property-type pages belongs to Plan 04. Add concise overview, expected outcomes, fit and not-fit guidance, timeline or sequencing signals, objection snippets, coordination notes, and FAQ references where they materially help a buyer evaluate the trade-specific services. Keep the language calm, specific, and semantically aligned between `uk` and `en`. Avoid inventing a separate segmentation system or dumping long essays into YAML.</action>
  <verify>bundle exec ruby -e 'require "yaml"; YAML.load_file("_data/service_pages.yml")'</verify>
  <done>Representative service entries now carry structured buyer-facing depth beyond the current summary/scope/process/proof fields.</done>
</task>

<task type="auto">
  <name>Task 2: Expand process-page commercial clarity without turning it into an operations manual</name>
  <files>_data/process_page.yml, _data/translations.yml</files>
  <action>Extend the process content with clearly public-facing sections for planning expectations, procurement support, coordination logic, readiness before the next stage, and handoff criteria. Make procurement a supporting topic, not the dominant narrative. Keep discussion of changes and budget drift visible only to the extent needed to reassure buyers that scope and decisions are controlled. Add shared translation labels only where the new module wrappers or summary blocks require them.</action>
  <verify>bundle exec ruby -e 'require "yaml"; %w[_data/process_page.yml _data/translations.yml].each { |path| YAML.load_file(path) }'</verify>
  <done>The process data now explains how work is planned, coordinated, and handed over in a way that directly supports CONT-03.</done>
</task>

<task type="auto">
  <name>Task 3: Render the deeper service and process modules with scan-first composition</name>
  <files>_layouts/service_detail.html, _layouts/process.html, _layouts/home.html, assets/css/site.css, scripts/qa.sh, scripts/phase2_render_checks.mjs</files>
  <action>Implement the richer service-detail and process sections using the new shared data structures. Compose them with short section intros, compact lists, summary bands, and subsection rhythm so pages feel richer without becoming hard to scan. Use the homepage only to surface a compact summary or bridge into the deeper pages, not to mirror the full depth. Update CSS only as needed to keep dense sections legible on mobile and desktop within the existing visual system. Extend rendered checks so representative service and process routes must show the new depth and commercial-clarity markers in both languages.</action>
  <verify>./scripts/qa.sh</verify>
  <done>Service-detail and process routes now deliver materially richer decision-supporting content, and the homepage reinforces the path without turning into a copy pile.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./scripts/qa.sh`
- [ ] `_site/process/index.html` and `_site/en/process/index.html` contain Phase 2 commercial-clarity and objection-ready markers.
- [ ] Representative service routes such as `_site/services/plumbing/index.html`, `_site/services/electrical/index.html`, `_site/services/apartment-renovation/index.html`, and `/en/` equivalents contain the required depth markers.
- [ ] Manual rendered review confirms the new sections scan through intros, lists, and subsections rather than uninterrupted long prose.
- [ ] Manual review confirms procurement and handoff are clear but do not dominate the page narrative.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Key service and process routes answer richer scope, process, quality, timing, and coordination questions
- Commercial expectations are visible and calming without reading like a public operating manual
  </success_criteria>

<output>
After completion, create `.planning/phases/02-content-architecture-expansion/02-02-SUMMARY.md`
</output>
