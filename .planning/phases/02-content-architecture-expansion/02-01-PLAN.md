---
phase: 02-content-architecture-expansion
plan: 01
type: execute
tool_mode: repo_only
mcp_usage: intentionally_not_needed
wave: 1
depends_on: []
files_modified:
  - _data/service_pages.yml
  - _data/process_page.yml
  - _data/faqs.yml
  - _layouts/home.html
  - _layouts/page.html
  - _layouts/process.html
  - _layouts/service_detail.html
  - scripts/qa.sh
  - scripts/phase2_render_checks.mjs
  - .planning/phases/02-content-architecture-expansion/02-VALIDATION.md
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
must_haves:
  truths:
    - Buyers should find richer, better-structured content on service-detail and process routes without losing the clear route hierarchy established in Phase 1.
    - Homepage and generic pages should remain supportive and scan-friendly while pointing visitors toward the deeper reading surfaces.
    - Buyers should encounter visible objection-handling and commercial-clarity surfaces consistently on the routes that will later carry the main Phase 2 content rollout.
    - Public-facing commercial guidance should explain planning, procurement, and handoff clearly without reading like an operations manual or a change-order playbook.
  artifacts:
    - _data/service_pages.yml
    - _data/process_page.yml
    - _data/faqs.yml
    - _layouts/home.html
    - _layouts/page.html
    - _layouts/process.html
    - _layouts/service_detail.html
    - scripts/qa.sh
    - scripts/phase2_render_checks.mjs
    - .planning/phases/02-content-architecture-expansion/02-VALIDATION.md
  key_links:
    - _data/service_pages.yml and _data/process_page.yml define the richer section contracts that later waves render on service and process routes.
    - _data/faqs.yml moves from a flat list toward grouped, referenceable objections so inline page modules and the standalone FAQ can share one source.
    - _layouts/home.html, _layouts/page.html, _layouts/process.html, and _layouts/service_detail.html expose stable Phase 2 markers that scripts/phase2_render_checks.mjs can validate.
    - scripts/qa.sh must call scripts/phase2_render_checks.mjs so Phase 2 regression coverage becomes part of the default repo QA path.
    - .planning/phases/02-content-architecture-expansion/02-VALIDATION.md is updated in the same wave so Wave 0 and Nyquist ownership do not drift behind implementation.
---

<objective>
Establish the Phase 2 execution guardrails and shared content schema before the site gains substantial new content density.

Purpose: This wave creates the validation and data-model foundation that makes later service, process, FAQ, and property-fit expansion executable without copy drift or bilingual regressions.
Output: A Phase 2 render-check script, QA wiring, richer bilingual data contracts for service/process/FAQ content, and stable layout hooks for later waves.
MCP status: intentionally not needed because this wave is repo-structure and validation work only.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-content-architecture-expansion/02-CONTEXT.md
@.planning/phases/02-content-architecture-expansion/02-RESEARCH.md
@.planning/phases/02-content-architecture-expansion/02-VALIDATION.md
@_data/service_pages.yml
@_data/process_page.yml
@_data/faqs.yml
@_layouts/home.html
@_layouts/page.html
@_layouts/process.html
@_layouts/service_detail.html
@scripts/qa.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Phase 2 QA guardrails and rendered markers first</name>
  <files>scripts/qa.sh, scripts/phase2_render_checks.mjs, _layouts/home.html, _layouts/page.html, _layouts/process.html, _layouts/service_detail.html</files>
  <action>Create `scripts/phase2_render_checks.mjs` and wire it into `scripts/qa.sh` after the Jekyll build succeeds. Add stable rendered markers for Phase 2 surfaces such as content-depth, objection-handling, commercial-clarity, fit-guidance, and FAQ-group visibility. Cover `/`, `/services/`, `/process/`, `/faq/`, representative service-detail routes, apartment and house routes, and their `/en/` equivalents. Preserve Phase 1 trust and phone-first checks by making Phase 2 automation additive, not a replacement. Extend the same Wave 0 guardrails to confirm translated-page metadata parity on touched bilingual routes, including `lang`, canonical, `hreflang`, and language-switch integrity.</action>
  <verify>./scripts/qa.sh</verify>
  <done>The default QA pipeline now fails when required Phase 2 markers are missing, when target bilingual routes lose depth/FAQ/fit surfaces, when translated-route metadata parity regresses, or when Phase 1 trust-call coverage regresses.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor shared bilingual content contracts for richer depth</name>
  <files>_data/service_pages.yml, _data/process_page.yml, _data/faqs.yml</files>
  <action>Extend the existing data files into Phase 2-ready contracts instead of adding page-local sprawl. `service_pages.yml` should gain optional modules for deeper overview, outcomes, fit boundaries, objection cards, coordination notes, commercial clarity, and FAQ references. `process_page.yml` should gain public-facing planning, procurement, handoff, and inline objection structures. `_data/faqs.yml` should move from a flat list to grouped question clusters with stable keys so the same answers can render inline and on the FAQ page. Keep YAML GitHub Pages-safe and require both `uk` and `en` branches for every new shared content branch used in this phase.</action>
  <verify>bundle exec ruby -e 'require "yaml"; %w[_data/service_pages.yml _data/process_page.yml _data/faqs.yml].each { |path| YAML.load_file(path) }'</verify>
  <done>Shared data files can now express the richer Phase 2 content architecture without duplicating long bilingual markup inside page files.</done>
</task>

<task type="auto">
  <name>Task 3: Add layout hooks that preserve scan-first page roles</name>
  <files>_layouts/home.html, _layouts/page.html, _layouts/process.html, _layouts/service_detail.html</files>
  <action>Prepare the layout layer to consume the new contracts while keeping the locked page-role decisions intact. Service-detail and process layouts must expose the deepest optional slots first. Home and generic pages may expose lighter supporting summaries, links, and FAQ teasers only if they do not turn into copy walls. Use modular section wrappers and concise intros so later waves can add density without abandoning the repo's data-driven structure or overwhelming the CTA path.</action>
  <verify>./scripts/qa.sh</verify>
  <done>The main layouts have Phase 2-ready section hooks and markers, and the site can grow depth through shared modules instead of ad hoc page rewrites.</done>
</task>

<task type="auto">
  <name>Task 4: Update the phase validation artifact with Wave 0 ownership</name>
  <files>.planning/phases/02-content-architecture-expansion/02-VALIDATION.md</files>
  <action>Update `02-VALIDATION.md` so Wave 0 requirements, per-task verification, metadata-parity checks, and final Nyquist closure reflect the actual Phase 2 guardrails implemented in this wave. Mark checks complete only when the new script, QA wiring, route coverage, translated-page metadata assertions, and bilingual data-integrity checks truly exist. Do not set `nyquist_compliant: true` until the final Phase 2 plan completes the full route and requirement coverage promised by the validation strategy.</action>
  <verify>rg -n "canonical|hreflang|language-switch|02-01-01|02-01-04" .planning/phases/02-content-architecture-expansion/02-VALIDATION.md</verify>
  <done>The validation artifact explicitly owns Phase 2 automation, route coverage, and sign-off gates instead of leaving them implied.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./scripts/qa.sh`
- [ ] `scripts/phase2_render_checks.mjs` validates `/`, `/services/`, `/process/`, `/faq/`, `/services/plumbing/`, `/services/electrical/`, `/services/apartment-renovation/`, `/services/house-renovation/`, and `/en/` equivalents.
- [ ] YAML integrity checks succeed for `_data/service_pages.yml`, `_data/process_page.yml`, and `_data/faqs.yml`.
- [ ] Rendered markers distinguish content-depth, objection-handling, commercial-clarity, fit-guidance, and FAQ-group surfaces without depending on fragile free-text matching.
- [ ] `.planning/phases/02-content-architecture-expansion/02-VALIDATION.md` records Wave 0 ownership for this plan.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 execution can proceed against enforceable shared data contracts and rendered route checks
- Service/process depth work is unblocked without sacrificing Jekyll safety or bilingual parity
  </success_criteria>

<output>
After completion, create `.planning/phases/02-content-architecture-expansion/02-01-SUMMARY.md`
</output>
