---
phase: 01-trust-and-phone-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - _data/trust_foundation.yml
  - _data/process_page.yml
  - _data/service_pages.yml
  - _includes/trust-strip.html
  - _includes/sourced-proof.html
  - _layouts/home.html
  - _layouts/page.html
  - _layouts/process.html
  - _layouts/service_detail.html
  - about/index.md
  - en/about/index.md
  - contact/index.md
  - en/contact/index.md
  - services/index.md
  - en/services/index.md
  - assets/css/site.css
  - scripts/qa.sh
  - scripts/phase1_render_checks.mjs
  - .planning/phases/01-trust-and-phone-foundation/01-VALIDATION.md
autonomous: true
requirements:
  - TRST-01
  - TRST-03
  - TRST-04
must_haves:
  truths:
    - Core pages communicate who is responsible, how stages are coordinated, and what accountability looks like before a buyer contacts the team.
    - Sourced proof appears inside the decision path with visible attribution or source labels instead of generic trust copy.
    - Ukrainian and English routes preserve the same trust and proof structure across home, process, services, and about surfaces.
    - Phase 1 cannot be considered complete unless at least one publishable sourced-proof item is live with visible attribution on core decision pages.
  artifacts:
    - _data/trust_foundation.yml
    - _includes/trust-strip.html
    - _includes/sourced-proof.html
    - _layouts/home.html
    - _layouts/process.html
    - _layouts/service_detail.html
    - _layouts/page.html
    - contact/index.md
    - en/contact/index.md
    - assets/css/site.css
    - scripts/phase1_render_checks.mjs
    - .planning/phases/01-trust-and-phone-foundation/01-VALIDATION.md
  key_links:
    - _data/trust_foundation.yml supplies identity, accountability, and proof-source content rendered by _includes/trust-strip.html and _includes/sourced-proof.html.
    - _data/process_page.yml and _data/service_pages.yml expose process and service-level accountability details that the updated layouts surface near CTAs.
    - _layouts/home.html, _layouts/process.html, _layouts/service_detail.html, _layouts/page.html, and the contact markdown pages keep proof and accountability modules on the main decision path in both languages.
    - scripts/phase1_render_checks.mjs is extended in this wave so rendered validation fails if sourced-proof modules or accountability blocks are missing from the required decision surfaces.
    - .planning/phases/01-trust-and-phone-foundation/01-VALIDATION.md is updated at the end of the wave so Wave 0 completion, per-task verification, and final Nyquist sign-off are not left dangling.
user_setup:
  - Provide real testimonial, review, credential, warranty, or equivalent proof items with source labels that can be published on the site.
  - Confirm any claims about operating history, guarantees, or accountability signals before they are added to trust modules or schema-adjacent copy.
  - If no publishable sourced proof exists yet, treat Phase 1 sign-off as blocked rather than substituting generic trust copy.
blocked_by:
  - Publishable sourced-proof items with visible attribution are required before this plan can be signed off as complete.
---

<objective>
Add the trust layer that shows business legitimacy, delivery accountability, and sourced proof across the main decision path without forcing users onto separate proof pages.

Purpose: This closes the remaining trust gap after the call system and first-call guidance are in place by replacing generic reassurance with operational clarity and attributable evidence.
Output: Shared trust and sourced-proof modules plus updated home, process, service, about, and services surfaces that explain control across renovation stages.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-trust-and-phone-foundation/01-CONTEXT.md
@.planning/phases/01-trust-and-phone-foundation/01-RESEARCH.md
@.planning/phases/01-trust-and-phone-foundation/01-VALIDATION.md
@.planning/phases/01-trust-and-phone-foundation/01-01-PLAN.md
@.planning/phases/01-trust-and-phone-foundation/01-02-PLAN.md
@_layouts/home.html
@_layouts/page.html
@_layouts/process.html
@_layouts/service_detail.html
@_data/process_page.yml
@_data/service_pages.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the trust and proof data model</name>
  <files>_data/trust_foundation.yml, _data/process_page.yml, _data/service_pages.yml</files>
  <action>Introduce `_data/trust_foundation.yml` for bilingual identity bullets, accountability framing, project-control signals, and sourced proof items with explicit attribution fields. Extend `_data/process_page.yml` and `_data/service_pages.yml` only where needed so process and service layouts can describe stage management, handoff logic, and proof relevance without page-specific duplication. Do not invent proof or credentials. Structure the data so rendered validation can distinguish between “publishable sourced proof present” and “phase blocked pending business inputs” rather than silently passing with generic placeholders. Add automated completeness expectations so every new trust data branch required for UA and EN renders fails QA if one language is missing.</action>
  <verify>bundle exec ruby -e 'require "yaml"; %w[_data/trust_foundation.yml _data/process_page.yml _data/service_pages.yml].each { |path| YAML.load_file(path) }'</verify>
  <done>The repo has a shared bilingual trust/proof contract, and process/service data expose the accountability details needed for visible trust modules.</done>
</task>

<task type="auto">
  <name>Task 2: Build reusable trust and sourced-proof modules</name>
  <files>_includes/trust-strip.html, _includes/sourced-proof.html, assets/css/site.css</files>
  <action>Create trust modules that feel editorial and specific, not generic. `trust-strip.html` should surface business identity, service profile, service area, and accountability cues. `sourced-proof.html` should render proof items with source labels, attribution, or other publishable evidence while avoiding review-schema misuse or invented ratings. Keep both modules compact enough to stay inside the decision path and adapt well to mobile and desktop. If there is no publishable sourced proof, the module and its surrounding logic must make that state explicit for planning/sign-off rather than silently faking completeness.</action>
  <verify>./scripts/qa.sh</verify>
  <done>The new modules render sourced, specific trust content and can be dropped into core layouts without breaking the current visual system.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy accountability and proof across core pages</name>
  <files>_layouts/home.html, _layouts/page.html, _layouts/process.html, _layouts/service_detail.html, about/index.md, en/about/index.md, contact/index.md, en/contact/index.md, services/index.md, en/services/index.md, scripts/qa.sh, scripts/phase1_render_checks.mjs</files>
  <action>Insert the trust and proof modules on the pages where buyers decide whether to call: homepage, process, service detail, about, services, and contact. Tie the content to stage control and accountability rather than decorative claims. On process and service pages, make proof relevance explicit to the page subject. On contact pages, place trust/proof near the first-call expectations so the final conversion surface still earns the ask. Extend rendered validation so this wave explicitly checks accountability-block visibility and sourced-proof visibility with attribution labels on the required decision surfaces in both languages, fails sign-off if publishable sourced proof is still absent, and fails if `_data/trust_foundation.yml` lacks either `uk` or `en` content for required modules.</action>
  <verify>./scripts/qa.sh</verify>
  <done>Core pages in both languages, including contact, show who is responsible, how scope is coordinated, and what proof is real, all without sending users off the main decision path, and validation cannot pass if sourced proof is still missing.</done>
</task>

<task type="auto">
  <name>Task 4: Complete Phase 1 validation ownership</name>
  <files>.planning/phases/01-trust-and-phone-foundation/01-VALIDATION.md</files>
  <action>Update `01-VALIDATION.md` so the Phase 1 validation contract is owned through completion. Mark Wave 0 requirements complete when the automation is in place, update the per-task verification map with the final commands/files/status expectations, and set the frontmatter fields needed for sign-off when the phase truly satisfies the validation strategy. Do not mark `nyquist_compliant: true` unless the rendered and data-integrity checks actually cover the full Phase 1 route surface and sourced-proof requirement.</action>
  <verify>test -f .planning/phases/01-trust-and-phone-foundation/01-VALIDATION.md</verify>
  <done>The validation artifact is updated alongside implementation so Wave 0 completion and final Nyquist sign-off are explicitly owned by the plan set.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./scripts/qa.sh`
- [ ] Rendered review confirms sourced proof appears on `/`, `/about/`, `/services/`, `/process/`, `/contact/`, `/services/plumbing/`, `/services/electrical/`, `/services/finishing/`, `/services/site-supervision/`, and their `/en/` equivalents with visible attribution labels where required.
- [ ] Manual review confirms trust copy names concrete identity and accountability signals rather than generic reassurance.
- [ ] `01-VALIDATION.md` is updated so Wave 0 completion and final validation state are explicitly owned.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Core pages explain scope coordination and accountability with publishable evidence
- Trust proof remains on the conversion path and keeps Ukrainian and English parity
  </success_criteria>

<output>
After completion, create `.planning/phases/01-trust-and-phone-foundation/01-03-SUMMARY.md`
</output>
