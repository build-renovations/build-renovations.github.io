---
phase: 01-trust-and-phone-foundation
plan: 04
type: execute
gap_closure: true
tool_mode: repo_only
mcp_usage: intentionally_not_needed
wave: 4
depends_on:
  - 01-03
files_modified:
  - _data/translations.yml
  - _data/trust_foundation.yml
  - _layouts/service_detail.html
  - _includes/trust-strip.html
  - .planning/phases/01-trust-and-phone-foundation/01-VALIDATION.md
autonomous: true
requirements:
  - TRST-01
  - TRST-03
  - TRST-04
must_haves:
  truths:
    - The homepage services heading on `/` and `/en/` reads naturally and still supports the same trust-first scope framing.
    - The service-detail bridge copy above the contextual trust block reads naturally on plumbing and other service pages in both languages.
    - The trust strip no longer shows `Статус бренду` or `Brand status`; company identity is labeled in a neutral, credible way or rendered cleanly without that label.
  artifacts:
    - _data/translations.yml
    - _data/trust_foundation.yml
    - _layouts/service_detail.html
    - _includes/trust-strip.html
  key_links:
    - "_data/translations.yml drives the homepage service-section heading on `/` and `/en/`."
    - "_data/trust_foundation.yml and _includes/trust-strip.html together control the company-label wording inside the shared trust strip rendered across Phase 01 routes."
    - "_layouts/service_detail.html owns the cross-service bridge sentence that currently reads awkwardly on `/services/plumbing/` and `/en/services/plumbing/`."
---

<objective>
Close the three minor UAT copy gaps logged after Phase 01 execution without replanning any completed system work.

Purpose: Tighten the remaining user-facing wording issues so the delivered Phase 01 trust layer reads naturally and credibly on the pages already covered by the completed plans.
Output: One focused copy-polish pass that updates the homepage heading, the service-detail bridge sentence, and the trust-strip company label treatment with UA/EN parity, while still running the Phase 01 QA contract because shared bilingual templates and data are being touched.
MCP status: intentionally not needed for this pass because the work is limited to repo-local copy polish and existing validation scripts.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-trust-and-phone-foundation/01-CONTEXT.md
@.planning/phases/01-trust-and-phone-foundation/01-RESEARCH.md
@.planning/phases/01-trust-and-phone-foundation/01-UAT.md
@.planning/phases/01-trust-and-phone-foundation/01-03-PLAN.md
@_data/translations.yml
@_data/trust_foundation.yml
@_layouts/service_detail.html
@_includes/trust-strip.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the two shared off-tone copy strings with tighter buyer-facing wording</name>
  <files>_data/translations.yml, _layouts/service_detail.html</files>
  <action>Update only the two strings called out by UAT: the homepage services heading and the service-detail bridge sentence above the contextual trust block. Keep the meaning intact: the homepage heading still needs to communicate full-scope renovation coverage, and the service-detail sentence still needs to position each service as part of a coordinated renovation system. Revise both Ukrainian and English so they read naturally rather than mechanically. Do not alter CTA hierarchy, layout structure, or unrelated section copy.</action>
  <verify>! rg -n "Комплексні роботи для квартир і будинків|End-to-end work for apartments and houses|Цей етап працює найкраще як частина пов'язаної системи|This stage works best as part of a connected system" _data/translations.yml _layouts/service_detail.html</verify>
  <done>The awkward homepage and service-detail phrases are replaced with cleaner UA/EN wording while preserving the original Phase 01 positioning.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the awkward trust-strip company label without reopening the trust module system</name>
  <files>_data/trust_foundation.yml, _includes/trust-strip.html</files>
  <action>Remove the `Статус бренду` and `Brand status` wording from the shared trust strip in the smallest credible way. Prefer a neutral company-identity label or a cleaner render that does not need the label at all, but keep the centralized placeholder note behavior and company field intact. Do not redesign the trust strip, change its card count, or rewrite unrelated accountability/proof copy.</action>
  <verify>! rg -n "Статус бренду|Brand status" _data/trust_foundation.yml _includes/trust-strip.html</verify>
  <done>The shared trust strip keeps its Phase 01 identity function but no longer shows the awkward brand-status wording on UA or EN routes.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm the gap closure stays narrow and renders cleanly on the affected routes</name>
  <files>_data/translations.yml, _data/trust_foundation.yml, _layouts/service_detail.html, _includes/trust-strip.html, .planning/phases/01-trust-and-phone-foundation/01-VALIDATION.md</files>
  <action>Run a narrow copy-surface smoke check first, then run the full Phase 01 QA contract plus a focused rendered review against the exact affected surfaces: `/`, `/en/`, `/services/plumbing/`, and `/en/services/plumbing/`. Confirm the new copy reads naturally in context, the old strings are absent, and no structural regressions were introduced while touching shared templates/data. Update `01-VALIDATION.md` so this gap-closure plan is explicitly covered by the phase validation artifact rather than bypassing Nyquist ownership.</action>
  <verify>./scripts/qa.sh</verify>
  <done>The three logged UAT copy issues are closed, the relevant UA/EN pages render successfully, the shared-template regression suite still passes, and `01-VALIDATION.md` records coverage for this gap-closure wave.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bundle exec jekyll build`
- [ ] `./scripts/qa.sh`
- [ ] `rg -n "Статус бренду|Brand status|Комплексні роботи для квартир і будинків|Цей етап працює найкраще як частина пов'язаної системи|This stage works best as part of a connected system" _site _data _layouts _includes` returns no remaining production matches for the retired copy.
- [ ] Manual rendered review of `/` and `/en/` confirms the service-section heading now reads naturally in context.
- [ ] Manual rendered review of `/services/plumbing/` and `/en/services/plumbing/` confirms the service-detail bridge sentence and trust-strip company treatment read naturally in context.
- [ ] `.planning/phases/01-trust-and-phone-foundation/01-VALIDATION.md` records verification coverage for `01-04`.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Only the three logged UAT copy issues are addressed in this wave
- Ukrainian and English remain semantically aligned after the copy polish
  </success_criteria>

<output>
After completion, create `.planning/phases/01-trust-and-phone-foundation/01-04-SUMMARY.md`
</output>
