---
phase: 01-trust-and-phone-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _config.yml
  - _data/contact_channels.yml
  - _data/translations.yml
  - _includes/header.html
  - _includes/footer.html
  - _includes/primary-phone-cta.html
  - _includes/contact-options.html
  - _layouts/default.html
  - _layouts/home.html
  - _layouts/process.html
  - _layouts/service_detail.html
  - assets/css/site.css
  - assets/js/site.js
  - scripts/qa.sh
  - scripts/phase1_render_checks.mjs
autonomous: true
requirements:
  - TRST-01
  - CALL-01
  - CALL-03
user_setup:
  - Confirm the working business name, phone number, and service area values to use in `_config.yml`, even if they remain explicitly placeholder content for now.
  - Confirm whether email stays visible as a tertiary contact method or should be removed from primary conversion surfaces.
must_haves:
  truths:
    - Visitors can start a phone call from the global shell on both desktop and mobile without detouring to the contact page first.
    - The homepage, process page, and service-detail page primary CTA surfaces no longer route their main action to `/contact/`; they start a call directly.
    - The mobile sticky phone CTA stays available on core routes without obscuring readable content or footer actions.
    - Canonical business identity and contact data come from one shared source that feeds both visible UI and schema-safe site config.
    - Placeholder identity and messenger values are allowed in Phase 1 only if they are centralized, intentional, and clearly tracked for later replacement.
  artifacts:
    - _config.yml
    - _data/contact_channels.yml
    - _includes/primary-phone-cta.html
    - _includes/contact-options.html
    - _includes/header.html
    - _layouts/default.html
    - _layouts/home.html
    - _layouts/process.html
    - _layouts/service_detail.html
    - assets/css/site.css
    - assets/js/site.js
    - scripts/qa.sh
    - scripts/phase1_render_checks.mjs
  key_links:
    - _config.yml and _data/contact_channels.yml drive the phone and messenger labels rendered by _includes/primary-phone-cta.html and _includes/contact-options.html.
    - _layouts/default.html and _includes/header.html expose the shared CTA includes on every route that uses the default shell.
    - _layouts/home.html, _layouts/process.html, and _layouts/service_detail.html explicitly rewire page-level primary CTA buttons so they call directly rather than detouring to `/contact/`.
    - scripts/qa.sh invokes scripts/phase1_render_checks.mjs so unlabeled placeholder identity data or missing tel links fail validation before release.
---

<objective>
Establish the shared contact and identity foundation for Phase 1 by moving the site to a phone-first global shell, adding a mobile-safe sticky call action, and hardening QA around shared contact data.

Purpose: This creates the reusable infrastructure every later trust block depends on, while fixing the current failure where primary CTAs do not start a call.
Output: Shared CTA includes, a placeholder-safe contact data contract, sticky mobile CTA behavior, and validation that fails on unlabeled placeholder identity or missing tel links.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-trust-and-phone-foundation/01-CONTEXT.md
@.planning/phases/01-trust-and-phone-foundation/01-RESEARCH.md
@.planning/phases/01-trust-and-phone-foundation/01-VALIDATION.md
@_config.yml
@_includes/header.html
@_includes/footer.html
@_layouts/default.html
@_data/translations.yml
@assets/css/site.css
@assets/js/site.js
@scripts/qa.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish Phase 1 guardrail automation first</name>
  <files>scripts/qa.sh, scripts/phase1_render_checks.mjs</files>
  <action>Implement the Phase 1 validation guardrails before the rest of the wave is considered verifiable. Add checks for missing or unlabeled placeholder company phone/email/business identity data, missing bilingual contact data, visible `a[href^=\"tel:\"]` links on `/`, `/about/`, `/services/`, `/process/`, `/contact/`, one representative service detail route, and their `/en/` equivalents, plus sticky CTA overlap and messenger-secondary hierarchy. Keep the helper script runnable in CI-style non-interactive mode and make `scripts/qa.sh` call it after the Jekyll build succeeds.</action>
  <verify>./scripts/qa.sh</verify>
  <done>The QA pipeline fails before later implementation can pass if placeholder identity is missing required placeholder labeling, bilingual contact data is incomplete, tel links are missing from required routes, or sticky CTA behavior regresses.</done>
</task>

<task type="auto">
  <name>Task 2: Create the shared contact and identity contract</name>
  <files>_config.yml, _data/contact_channels.yml, _data/translations.yml</files>
  <action>Replace scattered contact assumptions with a stable Phase 1 data contract. Keep canonical business facts in `_config.yml` so existing schema and global templates continue to work, then add `_data/contact_channels.yml` for bilingual phone and messenger presentation details. Preserve GitHub Pages-safe YAML only. Do not scatter phone labels or messenger URLs across layouts, and keep phone visually primary while any email copy stays explicitly lower-priority. Placeholder business identity values are allowed for this phase, but they must be clearly intentional, centralized, and easy to replace in later phases.</action>
  <verify>bundle exec ruby -e 'require "yaml"; %w[_config.yml _data/contact_channels.yml _data/translations.yml].each { |path| YAML.load_file(path) }'</verify>
  <done>`_config.yml` contains one centralized business identity contract, `_data/contact_channels.yml` exists with both `uk` and `en`, and translation keys needed by the new CTA shell resolve from shared data instead of hard-coded markup.</done>
</task>

<task type="auto">
  <name>Task 3: Build the global phone-first CTA shell</name>
  <files>_includes/header.html, _includes/footer.html, _includes/primary-phone-cta.html, _includes/contact-options.html, _layouts/default.html, _layouts/home.html, _layouts/process.html, _layouts/service_detail.html, assets/css/site.css, assets/js/site.js</files>
  <action>Introduce reusable `primary-phone-cta` and `contact-options` includes and wire them into the default shell. Convert the header and other global contact surfaces so the phone action is a real `tel:` link on desktop and mobile, then add a small-screen sticky CTA that stays reachable without covering body copy or footer links. Explicitly replace page-level primary CTA buttons in `_layouts/home.html`, `_layouts/process.html`, and `_layouts/service_detail.html` so the main action on those pages starts a call directly rather than routing through `/contact/`. Keep messenger actions visible but clearly secondary in hierarchy, and use lightweight vanilla JS only where needed for sticky behavior or menu coexistence.</action>
  <verify>./scripts/qa.sh</verify>
  <done>The default shell renders a visible `tel:` action across core routes, mobile shows a persistent phone CTA with safe spacing, and messenger links support the primary call path without matching its emphasis.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./scripts/qa.sh`
- [ ] Rendered checks explicitly cover `/`, `/about/`, `/services/`, `/process/`, `/contact/`, one representative service detail route, and their `/en/` equivalents.
- [ ] `_site/index.html`, `_site/about/index.html`, `_site/services/index.html`, `_site/process/index.html`, `_site/contact/index.html`, one representative service-detail page, and their `/en/` equivalents each contain at least one visible `tel:` action in rendered checks.
- [ ] Mobile rendered checks confirm the sticky CTA stays reachable without covering footer or page copy.
- [ ] `_config.yml` contains either real values or explicitly accepted placeholders for business name, phone, email, and service area, with no stray unlabeled placeholder data leaking into UI or JSON-LD.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The site shell exposes a shared phone-first CTA system instead of page-local contact links
- Unlabeled placeholder business identity values no longer pass QA
  </success_criteria>

<output>
After completion, create `.planning/phases/01-trust-and-phone-foundation/01-01-SUMMARY.md`
</output>
