---
phase: 05-seo-and-regression-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - _config.yml
autonomous: false
requirements:
  - SEO-01
  - SEO-02
  - SEO-03

must_haves:
  truths:
    - "No page on the site emits a `noindex` directive after the build"
    - "Business identity in `_config.yml` contains real company name, phone, and email — not placeholder values"
    - "`robots.txt` allows all crawlers and references the sitemap URL"
    - "`./scripts/qa.sh` passes in full (all phase checks green)"
    - "Service pages for apartment and house renovation have distinct `seo_description` values"
  artifacts:
    - path: "_config.yml"
      provides: "Real business identity and enabled indexing"
      contains: "allow_indexing: true"
  key_links:
    - from: "_config.yml seo.allow_indexing"
      to: "_includes/head.html robots meta conditional"
      via: "{% unless site.seo.allow_indexing %} toggle"
      pattern: "allow_indexing"
    - from: "_config.yml seo.allow_indexing"
      to: "robots.txt Liquid template"
      via: "{% if site.seo.allow_indexing %} toggle"
      pattern: "allow_indexing"
    - from: "_config.yml company.*"
      to: "_includes/head.html JSON-LD"
      via: "{{ site.company.name }}, {{ site.company.phone }}, {{ site.company.email }}"
      pattern: "site.company"

user_setup:
  - service: business-identity
    why: "Real company name, phone number, and email must be in _config.yml before the indexing flag is enabled. Placeholder values broadcast misleading structured data to search engines."
    env_vars: []
    dashboard_config:
      - task: "Provide the final company name to replace 'Business name pending confirmation'"
        location: "_config.yml → company.name"
      - task: "Provide the real phone number to replace '+380000000000'"
        location: "_config.yml → company.phone and company.phone_display"
      - task: "Provide the real contact email to replace 'hello@example.com'"
        location: "_config.yml → company.email"
---

<objective>
Replace placeholder business identity in `_config.yml` and flip `seo.allow_indexing` to `true` to make the site crawlable.

Purpose: This is the launch gate. Without real business identity, structured data misleads search engines. Without `allow_indexing: true`, the entire site is blocked regardless of metadata quality. Both changes happen in one plan to enforce the prerequisite ordering: identity first, then indexing.
Output: Updated `_config.yml` with real business data and `allow_indexing: true`. Full `qa.sh` suite passes including Phase 5 checks.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-seo-and-regression-hardening/05-RESEARCH.md
@.planning/phases/05-seo-and-regression-hardening/05-01-SUMMARY.md

<interfaces>
<!-- Relevant _config.yml structure to update. -->

From _config.yml (current placeholder state — all three values must be replaced before allow_indexing is set):
```yaml
company:
  name: "Business name pending confirmation"
  phone: "+380000000000"
  phone_display: "+380 (00) 000-00-00"
  email: "hello@example.com"
  city: "Lviv"
  country: "Ukraine"
  service_area:
    uk: "Львів і Львівська область"
    en: "Lviv and the surrounding region"

seo:
  allow_indexing: false   # ← flip to true AFTER identity is replaced
  type: HomeAndConstructionBusiness
```

Indexing toggle implementation (no template changes needed — these already exist):
```liquid
<!-- _includes/head.html -->
{% unless site.seo.allow_indexing %}
<meta name="robots" content="noindex, nofollow, noarchive">
<meta name="googlebot" content="noindex, nofollow, noarchive">
{% endunless %}

<!-- robots.txt -->
{% if site.seo.allow_indexing %}
User-agent: *
Allow: /
Sitemap: {{ '/sitemap.xml' | absolute_url }}
{% else %}
User-agent: *
Disallow: /
{% endif %}
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provide real business identity values</name>
  <files>_config.yml</files>
  <action>Wait for user to provide the four business identity values listed in required-inputs. Claude cannot supply these — they must come from the business owner. Do not proceed to Task 2 until all four values are provided.</action>
  <verify>
    <automated>MISSING — user must provide values before automated verification is possible</automated>
  </verify>
  <done>User has provided company name, phone (international format), phone display format, and email address.</done>
  <what-claude-will-do>Claude will update `_config.yml` with the values you provide here. This must happen before the indexing flag is flipped — placeholder values in live structured data can be flagged by Google as misleading.</what-claude-will-do>
  <required-inputs>
    1. **Company name** — the real business name to replace `"Business name pending confirmation"`
    2. **Phone number** — the real phone in international format (e.g., `+380671234567`) to replace `+380000000000`
    3. **Phone display** — formatted for display (e.g., `+380 (67) 123-45-67`) to replace `+380 (00) 000-00-00`
    4. **Email** — the real contact email to replace `hello@example.com`
  </required-inputs>
  <resume-signal>Provide the four values above and Claude will update `_config.yml` and flip the indexing flag.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update _config.yml with real business identity and enable indexing</name>
  <files>_config.yml</files>
  <action>
    Using the values provided by the user in Task 1:

    1. Replace `company.name` value with the real company name (keep the YAML string in quotes)
    2. Replace `company.phone` with the real international-format phone number
    3. Replace `company.phone_display` with the real display-formatted phone number
    4. Replace `company.email` with the real email address
    5. Set `seo.allow_indexing: true`

    Do NOT change any other fields — `city`, `country`, `service_area`, `type`, or any other keys must remain unchanged.

    After editing, run a full build and verify:
    - `_site/index.html` does NOT contain `noindex`
    - `_site/robots.txt` contains `Allow: /` and `Sitemap:` line
    - `_site/index.html` JSON-LD does NOT contain `Business name pending confirmation` or `+380000000000`
    - `./scripts/qa.sh` passes all checks including Phase 5
  </action>
  <verify>
    <automated>cd /Users/mykhailomykhailenko/work/personal/vasyl/remonty && RUBYOPT="-r./scripts/jekyll_compat.rb ${RUBYOPT:-}" bundle exec jekyll build --quiet 2>/dev/null && (grep -q "noindex" _site/index.html && echo "FAIL: noindex still present" || echo "ok: no noindex on home") && grep "Allow:" _site/robots.txt && node ./scripts/phase5_render_checks.mjs</automated>
  </verify>
  <done>`_site/index.html` contains no `noindex` directive. `_site/robots.txt` contains `Allow: /` and a `Sitemap:` reference. `node ./scripts/phase5_render_checks.mjs` passes. `./scripts/qa.sh` exits 0.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `_config.yml` contains no placeholder values for `company.name`, `company.phone`, or `company.email`
2. `_config.yml` has `seo.allow_indexing: true`
3. `bundle exec jekyll build` succeeds
4. `_site/index.html` has no `<meta name="robots" content="noindex...">` tag
5. `_site/robots.txt` allows crawling and references `sitemap.xml`
6. `./scripts/qa.sh` exits 0 (all phase checks including Phase 5 pass)
</verification>

<success_criteria>
- `_config.yml` has real company identity and `allow_indexing: true`
- Full `./scripts/qa.sh` passes — this is the primary Phase 5 automated gate
- No placeholder patterns appear in built HTML JSON-LD blocks
- `robots.txt` allows all crawlers
</success_criteria>

<output>
After completion, create `.planning/phases/05-seo-and-regression-hardening/05-02-SUMMARY.md`
</output>
