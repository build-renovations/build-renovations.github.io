---
phase: 05-seo-and-regression-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified: []
autonomous: false
requirements:
  - SEO-01
  - SEO-02
  - SEO-03

must_haves:
  truths:
    - "All Phase 5 automated checks pass before this plan runs"
    - "Visitor sees no `noindex` trace in page source on any checked route"
    - "Service pages show distinct, intent-specific SEO descriptions for apartment and house renovation"
    - "FAQ page source contains FAQPage structured data"
    - "Site is confirmed ready for search engine indexing"
  artifacts:
    - path: "_site/robots.txt"
      provides: "Crawler access with sitemap reference"
      contains: "Allow: /"
    - path: "_site/sitemap.xml"
      provides: "Full page index for search engine discovery"
  key_links:
    - from: "_site/robots.txt"
      to: "_site/sitemap.xml"
      via: "Sitemap: directive pointing to sitemap URL"
      pattern: "Sitemap:"
---

<objective>
Final rendered QA verification — confirm the site is release-ready from an SEO and regression standpoint.

Purpose: Human-verified sign-off that automated checks pass, rendered pages look correct, and the site is safe to index. This is the Phase 5 completion gate.
Output: Phase 5 sign-off. Site marked ready for live indexing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-seo-and-regression-hardening/05-01-SUMMARY.md
@.planning/phases/05-seo-and-regression-hardening/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full QA suite and gates</name>
  <files></files>
  <action>
    Run the full automated suite in order:

    1. `RUBYOPT="-r./scripts/jekyll_compat.rb ${RUBYOPT:-}" bundle exec jekyll build`
    2. `./scripts/qa.sh` — must exit 0 (all phase checks including Phase 5)
    3. `./scripts/gates.sh gate4` — Lighthouse SEO score >= 95 on UA and EN home routes, no broken links

    If any check fails, report the specific failure message and stop. Do not proceed to the checkpoint if the automated suite is not green.

    After both commands pass, print a summary of what was verified:
    - Which routes were checked by phase5_render_checks.mjs
    - Lighthouse SEO score on `/` and `/en/`
    - Confirmation that `_site/robots.txt` contains `Allow: /` and `Sitemap:` reference
    - Confirmation that `_site/sitemap.xml` is present and non-empty (print line count)
  </action>
  <verify>
    <automated>cd /Users/mykhailomykhailenko/work/personal/vasyl/remonty && RUBYOPT="-r./scripts/jekyll_compat.rb ${RUBYOPT:-}" bundle exec jekyll build --quiet 2>/dev/null && ./scripts/qa.sh</automated>
  </verify>
  <done>`./scripts/qa.sh` exits 0. `./scripts/gates.sh gate4` exits 0. Summary of verified routes printed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sign-off on SEO and release readiness</name>
  <files></files>
  <action>Present the automated QA summary to the user and request sign-off. List all verified behaviors. Surface any known cosmetic issues from Phase 4 UAT so the user is not surprised. Wait for explicit approval before creating the Phase 5 SUMMARY.</action>
  <verify>
    <automated>MISSING — this is a human-judgment gate; automated checks completed in Task 1</automated>
  </verify>
  <done>User has typed "approved" or equivalent confirmation that the site is ready for indexing.</done>
  <what-built>
    Phase 5 has delivered:
    - SEO-01: `allow_indexing: true`, canonical and hreflang verified across all major bilingual route pairs, sitemap.xml present
    - SEO-02: Distinct seo_description values on apartment-renovation and house-renovation service pages, all existing service intents preserved
    - SEO-03: `Service` JSON-LD on service-detail pages, `FAQPage` JSON-LD on the FAQ page, real business identity in structured data
    - Render check: `scripts/phase5_render_checks.mjs` registered in `qa.sh` covering all SEO assertions
  </what-built>
  <how-to-verify>
    1. Open `_site/index.html` in a text editor and confirm no `noindex` in `<head>`
    2. Open `_site/robots.txt` — confirm `Allow: /` and a `Sitemap:` line pointing to `sitemap.xml`
    3. Open `_site/services/plumbing/index.html` — find `"@type": "Service"` in the `<head>` JSON-LD
    4. Open `_site/faq/index.html` — find `"@type": "FAQPage"` in the `<head>` JSON-LD
    5. Open `_site/index.html` — confirm `"@type": "FAQPage"` does NOT appear
    6. Open `_config.yml` — confirm `company.name`, `company.phone`, `company.email` contain real values (not placeholders)
    7. (Optional) Paste `_site/faq/index.html` content into https://search.google.com/test/rich-results to validate FAQPage schema

    Known cosmetic issues from Phase 4 UAT (not introduced in Phase 5, do not block sign-off):
    - Slight header overlap on some routes at certain viewport widths
    - Spacing gaps on /process/ and service pages
    - Projects index visual gaps
    - EN copy quality (cosmetic, not structural)
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 5, or describe any SEO or release-readiness issues that need fixing.</resume-signal>
</task>

</tasks>

<verification>
Phase 5 is complete when:
1. `./scripts/qa.sh` exits 0 including Phase 5 checks
2. `./scripts/gates.sh gate4` exits 0 (Lighthouse SEO >= 95)
3. Human has confirmed real business identity, indexing enabled, and schema presence
4. Phase 5 sign-off recorded in SUMMARY
</verification>

<success_criteria>
- Full automated suite (qa.sh + gates.sh gate4) passes
- Human confirms indexing enabled, business identity real, Service and FAQPage schema present
- No blocking issues raised in checkpoint
- Phase 5 SUMMARY created with sign-off
</success_criteria>

<output>
After completion, create `.planning/phases/05-seo-and-regression-hardening/05-03-SUMMARY.md`
</output>
